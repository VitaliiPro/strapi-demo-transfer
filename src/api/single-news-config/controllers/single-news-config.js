'use strict';

/**
 * single-news-config controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::single-news-config.single-news-config');
