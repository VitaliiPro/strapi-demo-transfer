'use strict';

/**
 * single-news-config service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::single-news-config.single-news-config');
